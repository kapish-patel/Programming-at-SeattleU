class WorkoutModel{
  String emoji;
  String workoutName;
  int hours;
  int minuites;
  DateTime dateTime;
  String uuid;

  WorkoutModel(this.emoji, this.workoutName, this.hours, this.minuites, this.dateTime, this.uuid); 
}