class UserModel{

  int decidationLevel;
  int recordingPoints; 
  DateTime lastRecorded;
  String lastRecordedString;
  String uuid;

  UserModel(this.decidationLevel, this.recordingPoints, this.lastRecorded, this.lastRecordedString, this.uuid);
}